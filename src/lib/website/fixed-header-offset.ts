/** Spazio sotto header `fixed` (banner + navbar). Da applicare al `<main>` delle pagine interne che non usano hero full-bleed come la home. */
export const WEBSITE_FIXED_HEADER_MAIN_PT =
    "pt-[104px] sm:pt-[112px]"
